diff --git a/drivers/spi/spi_esp32_spim.c b/drivers/spi/spi_esp32_spim.c
index e6272af02d5..e954806bf04 100644
--- a/drivers/spi/spi_esp32_spim.c
+++ b/drivers/spi/spi_esp32_spim.c
@@ -43,4 +43,11 @@ LOG_MODULE_REGISTER(esp32_spi, CONFIG_SPI_LOG_LEVEL);
-#define SPI_DMA_MAX_BUFFER_SIZE 4092
+/* ESP32-S3 has an 18-bit transfer-length register. Keep each transaction
+ * within that limit while allowing GDMA to chain eight 4092-byte descriptors.
+ */
+#if defined(CONFIG_SOC_SERIES_ESP32S3)
+#define SPI_DMA_MAX_BUFFER_SIZE (4092 * 8)
+#else
+#define SPI_DMA_MAX_BUFFER_SIZE 4092
+#endif
 
 #define SPI_DMA_RX 0
 #define SPI_DMA_TX 1
