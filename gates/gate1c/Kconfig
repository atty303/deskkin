# SPDX-License-Identifier: MIT

mainmenu "Deskkin Gate 1C"

source "Kconfig.zephyr"

config DESKKIN_GATE_MODE
	int "Gate 1C probe mode"
	range 1 2
	default 1
	help
	  Use 1 for the ABI and idle probe and 2 for the deliberate panic probe.
